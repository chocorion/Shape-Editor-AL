package application.view;

public class TopBarView extends ViewDecorator {
    public TopBarView(View view) {
        super(view);
    }

    @Override
    public void draw() {
        super.draw();

        // draw topBar
    }
}
