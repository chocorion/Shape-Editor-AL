package application.view;

public class ToolBar extends ViewDecorator {
    public ToolBar(View view) {
        super(view);
    }

    @Override
    public void draw() {
        super.draw();

        // Draw toolbar
    }
}
