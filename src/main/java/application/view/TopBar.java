package application.view;

public class TopBar extends ViewDecorator {
    public TopBar(View view) {
        super(view);
    }

    @Override
    public void draw() {
        super.draw();

        // draw topBar
    }
}
